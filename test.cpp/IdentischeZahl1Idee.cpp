#include <iostream>

void noIdea () {
    int num_Dice = 5;
    // wenn gleiche zahlen
    for (int j = 0; j < num_Dice; j++) {
        for (int k = j + 1; k < num_Dice; k++) { // wenn k = j, + 1 damit nicht mit sich selsbt vergleicht
            //if (dice[j] == dice[k]) {
              //  same_num = true;
                //std::cout << "gleiche zahl" << " " << dice[j] << " " << dice[k] << std::endl;
            }
            
        }
    }